import React from 'react';

const TodoItem = ({
  tid,
  title,
  deadlineTime,
  fileName,
  fileUploadTimestamp,
  fileId,
  comment,
  isCompleted,
  isRemarkable,
  lastUpdated
}) => {
  return (
    <div />
  );
};

export default TodoItem;
